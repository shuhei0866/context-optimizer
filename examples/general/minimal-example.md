# General minimal example

Define a repetitive support task and collect:

- call count
- token usage per call
- pass/fail based on strict schema

Then run one variant candidate at a time and remove low-impact prompt chunks.
