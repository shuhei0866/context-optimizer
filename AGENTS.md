# AGENTS

このドキュメントは、このリポジトリを扱う AI エージェント向けの運用指針です。

## 方針
- 変更は必要最小限にする。
- 破壊的操作（`git reset --hard`、`git checkout --`、`rm -rf` など）は行わない。
- 明示的な依頼がない限り、テスト実行・検証・再読込みは行わない。
- ユーザーが確認中の作業は一度に完結させる（不要な差分の往復を避ける）。
- 既存のフォーマット・命名・構造を乱さない。

## 指示優先順位
- 先にユーザー指示、次にシステム/開発者指示、次に本 `AGENTS.md`。
- 競合がある場合は上位指示を優先する。

## コード編集ルール
- 単発の編集は `apply_patch` を優先。
- 大量生成が必要な場合を除き、単純な差分は最小単位で適用する。
- ASCII を標準とし、追加文字は必要時のみ。
- ファイル作成・編集後は原則として同一ファイルの再読は避ける。

## 安全運用
- 依存関係の変更・外部ネットワーク操作・環境設定変更は、ユーザー確認が必要。
- 仕様・データに影響する変更では、影響範囲を短く明示する。
- 変更は、できるだけ既存のテスト/ドキュメントとの整合を保つ。

## レビュー・出力期待
- まず結果を簡潔に述べる。
- 追加で必要なら次アクション（例: 参照用テンプレ追加、リンク追加、運用ルール拡張）を提示する。

## 運用カテゴリ別ルール

### 1) 実装依頼
- 目的に必要な最小範囲で差分を作る。
- 仕様変更を伴う場合は、影響を受けるファイルを短く明記する。
- 変更後は要点を先に報告し、必要なら次の確認タスクを提示する。

### 2) レビュー依頼
- 変更点の妥当性、再現性、バグリスクを優先して指摘する。
- 結果よりも想定リスクを先頭に書く。
- 既存指針との整合確認を簡潔に行う。

### 3) 文書・ルール更新
- 方針文書（`AGENTS.md` 含む）を更新する場合は、既存ルールを壊さないことを最優先にする。
- 参考リンクや運用ルートは「参照先を明示」して追記する。

## AI向け参照ドキュメント
- 数式記法（Obsidian 参照）: `docs/obsidian-kfluid-math-for-ai.md`
